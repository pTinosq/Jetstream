# Drizzle
DATABASE_URL=local.db

# Flight auto-detect (optional). Create a free OpenSky Network account at
# https://opensky-network.org/ and add an API client to get these. Without
# them, flights are entered manually.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
